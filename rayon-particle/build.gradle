sourceSets {
    testmod {
        compileClasspath += main.compileClasspath
        runtimeClasspath += main.runtimeClasspath
    }
}

dependencies {
    api project(path: ':rayon-core', configuration: 'shadow')
    testmodImplementation sourceSets.main.output
}